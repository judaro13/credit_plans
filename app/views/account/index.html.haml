
%p.navbar-text.pull-right
- unless current_user
  .jumbotron
    .row
      .col-lg-8
        %h1 Bienvenido 
        %p Con esta aplicación usted puede proveer diversos planes de credito para sus clientes.
        %p
          = link_to "Cree una nueva cuenta ", "/users/sign_up", class: "btn btn-primary btn-lg"
      .col-lg-4
        .panel.panel-primary
          .panel-heading
            %h3.panel-title Ingresar al sistema
          .panel-body
            = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
              %div
                = f.label :email
                %br/
                = f.email_field :email, autofocus: true
              %div
                = f.label "Contraseña"
                %br/
                = f.password_field :password, autocomplete: "off"
              - if devise_mapping.rememberable?
                %div
                  = f.check_box :remember_me
                  = f.label :remember_me
              %div= f.submit "Ingresar", class: 'btn'
            = render "devise/shared/links"